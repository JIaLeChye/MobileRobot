from setuptools import setup

setup(
    name='RPi_Robot_Hat_Lib',
    version='1.0.0',
    py_modules=['RPi_Robot_Hat_Lib'],  # Use packages=['RPI_HAT_Lib'] if it's a package
    install_requires=[],  # Add dependencies if any
)
